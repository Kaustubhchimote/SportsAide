import { memo, SVGProps } from 'react';

const Vector14Icon = (props: SVGProps<SVGSVGElement>) => (
  <svg preserveAspectRatio='none' viewBox='0 0 2 126' fill='none' xmlns='http://www.w3.org/2000/svg' {...props}>
    <path d='M0 0V125.573' stroke='#EEEFF2' strokeWidth={2} strokeLinecap='round' />
  </svg>
);

const Memo = memo(Vector14Icon);
export { Memo as Vector14Icon };
